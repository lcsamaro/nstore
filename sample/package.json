{
  "name": "sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "datascript": "^0.16.2",
    "mithril": "^1.1.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.0",
    "webpack": "^3.6.0",
    "webpack-dev-server": "^2.8.2"
  },
  "scripts": {
    "build": "webpack -p",
    "watch": "webpack --watch -p --optimize-minimize",
    "dist": "webpack -p --optimize-minimize",
    "dev": "(mkdir logs || true) && ( openresty -p . -c ./nginx.conf -s stop || true ) && ( openresty -p . -c ./nginx.conf ) && webpack-dev-server --port 8081 --colors",
    "stop": "openresty -p . -c ./nginx.conf -s stop",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
